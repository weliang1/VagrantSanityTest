kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: allow-from-red-to-blue
spec:
  podSelector: 
    matchLabels:
      name: blue-openshift
  ingress:
    - from:
      - podSelector:
          matchLabels:
            name: red-openshift
