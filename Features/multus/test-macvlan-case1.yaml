## Use “oc edit networks.operator.openshift.io cluster” to configure MACLVAN

additionalNetworks:
  # test-macvlan-case1 (all-default)
  # master interface: default route interface
  # mode: bridge
  # ipam: DHCP
  - name: test-macvlan-case1
    type: SimpleMacvlan

